# Protocol implementation rules

This is the detailed, on-demand companion to `AGENTS.md`. Each binding rule
names the specification rule it implements, the code that holds it, and the
test that would fail if it stopped holding. Read the relevant row when
changing that mechanism; workflow and handoff rules live in `AGENTS.md` and
`WORK.md`. Plain terms only, in the words Construction uses.

**Where the code stands.** Construction's core claim layer is the shielded
pool (§C1.2), built here as construction `moe/pool/v2` (`pool-v2.md`): the
domain-only configuration, the public scope with private membership, the
segment, one anchor per input, finalized import. This repository's
transparent path — `ledger.ts`, `sequencer.ts`, `presentation.ts`,
`replacement.ts`, `recovery.ts`, `fault.ts` — implements the transparent
*profile* (Extensions) and is **frozen**: it is a differential oracle and a
library of adversarial cases while the pool path is built, and it is deleted
when the pool path passes those cases. Rows marked *frozen* describe what
that code does today, including two mechanisms the specification has since
retired. Rows marked *core* bind the pool path too. Rows marked *model only*
are rules the executable model checks and the runtime does not yet hold;
the historical `moe/pool/v1` runtime was replaced in this repository by v2
and is retrievable from git history only.

Runtime rows follow the pinned v2 specification in the README. Later fault
rules are model-only refinements; they do not reinterpret v2 commitments.

## Binding rules

Pool demand attribution follows [C3.3a at 923ee46](https://github.com/mediumofexchange/money-from-first-principles/blob/923ee46/pool-recovery.md):
the holding proof authorizes the bound notice; it does not prove presenter-key
participation or publisher/civil identity. `model/pool-recovery.test.ts` has
focused copy, metadata-rebinding, exit-authority and replay cases. Proof and
signature authentication there remain ideal; the selected v3 proof system's
full public-input binding and real signatures remain runtime obligations.
The v2 runtime and signed transparent profile keep their pinned rules.

Rules can change through the decision and review process in AGENTS.md.
Amend the specification first, then this map and dependent code. Implementation
changes must not silently diverge from an unchanged rule.

| Rule | Spec | Code | Test | Scope |
|---|---|---|---|---|
| Candidate configuration fixes all six bytecode/key identities, helper, bounds and delivery profile; keys come from an independent manifest. Canonical signed constant-root terms bind the backing name, configuration, venue and issuance key. These checks do not establish adoption, terms force or spendability. | [pool-v3 §11 at 916bffb](https://github.com/mediumofexchange/money-from-first-principles/blob/916bffb/pool-v3.md#11-configuration-and-backing-evidence-before-adoption), C1.3, C4.6 | `model/pool-v3-configuration.ts`, `model/pool-v3-terms.ts`, `scripts/pool/v3/candidate.mjs`, `local-replay.mjs` | `pool-v3-configuration`, `pool-v3-terms`, `scripts/pool/v3/local-check.mjs` | candidate conformance only; production v2 still refuses v3 |
| Receiver requests retain a fresh secret before sharing the owner; pending statements and input reservations commit before submission. Local fulfillment binds exact positive output, requested owner and amount, authenticated receipt and independently replayed checkpoint inclusion; missing evidence changes no fulfillment state. | pool-v2 §3, §9; inv 25–26 | `pool/wallet.ts`, `pool/wallet-store.ts` | `test/pool-wallet.test.ts`, `scripts/pool/wallet/check.mjs`, `scripts/pool/wallet/crash.mjs` | local v2 wallet custody; real proofs and abrupt commit-boundary acceptance with a fixture venue, no rollback or external fulfillment guarantee |
| Receiver note status requires a matching positive requested opening, local owner secret, output inclusion and nullifier status in verified event closure. Unspent is relative to the exact selected checkpoint; an older prefix is not latest state. Saved fulfillment lookup recovers the original record without another credit. | pool-v2 §3, §10; inv 25–26 | `pool/wallet-store.ts` `checkNote`, `fulfillment` | `test/pool-wallet.test.ts`, `scripts/pool/wallet/check.mjs` | read-only local wallet checks; historical fulfillment remains distinct |
| Ordinary payments select at most two same-backing unspent unreserved fulfilled notes after one checkpoint replay; local paths read only verified local events. Real nullifiers sort before deterministic derivation. One paired owner has one payment alias; exact saved retry preserves proof/output bytes. Positive change needs an unused internal request rechecked inside reservation commit. | pool-v2 §3, §4, §7.2, §9; inv 25–26 | `pool/wallet-payment.ts`, `pool/wallet-store.ts`, `pool/wallet.ts` | `test/pool-wallet-operation.test.ts`, `scripts/pool/wallet/operation-check.mjs` | local profile; one segment without imports, historical evidence and no automatic reservation release |
| Private delivery uses authenticated HTTPS and an invoice-scoped capability. Bounded canonical frames must match the request and original receipt attestation before immutable inbox storage; exact retries return the saved hash. Acknowledgment means durable storage, never proof verification or fulfillment. | pool-v2 §3, §9; local application profile | `pool/wallet-delivery-wire.ts`, `pool/wallet-delivery-http.ts`, `pool/wallet-pairing.ts`, `pool/wallet-store.ts` | `test/pool-wallet-delivery.test.ts`, `test/pool-wallet-pairing.test.ts`, `test/pool-wallet.test.ts`, wallet CLI and crash acceptance | private credentials and exact invitation digests, atomically fenced rotation; independent user authentication remains unqualified; protocol changed-proof retry semantics remain unchanged |
| Served trails carry a canonical header, every scoped term/signature and exact local records with checked outer boundaries and explicit byte/event budgets. Local authentication binds the header and ordered event evidence to an externally authenticated snapshot; it grants no terms validity, imported state or verdict. | [pool-v3 §10 at 7ea0ee8](https://github.com/mediumofexchange/money-from-first-principles/blob/7ea0ee8/pool-v3.md#10-served-trail-transport), C2.10.10–13 | `model/pool-v3-trail.ts` | `test/pool-v3-trail.test.ts` | transport and local evidence authentication only; complete certificate dependencies and replay remain open |
| Portable fault evidence hashes exact raw target statement/proof/authorization fields and consecutive suffix triples against an externally authenticated snapshot. Contexts, indices, field bounds, exact size and local suffix budgets precede payload allocation/hashing. Resource refusal does not classify a checkpoint. | [pool-v3 §9 at 322bcae](https://github.com/mediumofexchange/money-from-first-principles/blob/322bcae/pool-v3.md#9-fault-evidence-records), C2.10.10–13 | `model/pool-v3-fault-evidence.ts` | `test/pool-v3-fault-evidence.test.ts` | portable evidence authentication only; complete certificate dependencies, replay and adoption remain open |
| Segment headers bind domain, venue, operator, first sequence, ordered scope and exact openings. Count and byte length are bounded before entry decoding; one empty sentinel and same-operator predecessor ordering are enforced. Structural decoding grants no authority or opening state. | [pool-v3 §8 at 061f87e](https://github.com/mediumofexchange/money-from-first-principles/blob/061f87e/pool-v3.md#8-segment-headers), C2.10.1–5 | `model/pool-v3-headers.ts` | `test/pool-v3-headers.test.ts` | byte conformance only; complete openings, replay and adoption remain open |
| Snapshot digests bind separate semantic and exact-evidence chains. Authentication hashes actual failing proof/authorization fields without legitimizing them. Receipts bind the five event comparison values; adoption keeps source statement evidence while receipts name the adopting segment. A suffix authenticates only against a previously authenticated snapshot digest. | [pool-v3 §7 at 4a58fdc](https://github.com/mediumofexchange/money-from-first-principles/blob/4a58fdc/pool-v3.md#7-history-evidence-snapshots-and-receipts), C2.10.10–13, C2b.4.2 | `model/pool-v3-commitments.ts`, `model/pool-v3-records.ts` | `test/pool-v3-commitments.test.ts` | byte conformance with real signature tests; no runtime classification, adopted configuration or certificate transport |
| Canonical successor statement/publication records fix exact domains, counts, integer ranges, authorization slots and capsule vectors. Withdrawal signatures bind their statement; settle reconstructs acceptance/release messages. Exact evidence hashes empty proof/authorization as zero digests. Indirect backing routing still needs the demand. | [pool-v3 §§5–6 at ca727f6](https://github.com/mediumofexchange/money-from-first-principles/blob/ca727f6/pool-v3.md#5-canonical-statement-records), C3.4–6, C4.4, C2b.3.2 | `model/pool-v3-records.ts` | `test/pool-v3-records.test.ts` | byte conformance only; synthetic domain/proof fixtures, no runtime adoption or state/force verification |
| Classify checkpoints at their original record prefix. Pass authenticated exclusions without supplying state, imports, receipt boundaries or sequence holes; refuse unresolved evidence. Continue only from the last valid prefix, and never discard an honest live tail merely because a checkpoint was excluded. | C2.10.9c, C2.10.11–13 | `model/pool-fault.ts` | `model/pool-fault.test.ts`, `model/pool-fault-reader.test.ts`, `model/pool-fault-alternatives.test.ts` | model only |
| Hash exact admitted proof/signature bytes separately from complete public statement identity. Authenticate supplied evidence before attributing failure; preserve valid-prefix evidence. Both receipt walks compare exact evidence digests. Adoption retains the original forceful publication's bytes at the new position without rejudging force. | C2.10.10, C2b.4.2 | `model/pool-evidence.ts`; `model/pool-authority.ts` `ProofOracle`; `model/pool-fault.ts` | `model/pool-evidence.test.ts`, `model/pool-evidence-reader.test.ts`, `model/pool-adopted-evidence.test.ts` | model only; real hashes with ideal proof/authentication oracles, not v3 encoding |
| A backing's no-commitment clock is its last valid carrying snapshot's index. Non-carrying, excluded and lapsed checkpoints reset nothing; unresolved carrying evidence blocks. Silence still retires continuation and lapses the unfinished tail. | C2b.6.1, C2b.4.1/3 | `model/pool-fault.ts` `closing`; `model/pool-recovery.ts` | `model/pool-fault-alternatives.test.ts`, `model/pool-silence.test.ts` | model only; rejected clocks remain a historical test helper |
| The note and nullifier domain is the configuration hash, which names neither operator nor segment; every hidden input backing, padding included, proves membership in the public scope under its link, and every output names an input's backing. | C1.2.1–2, pool-v2 §§2, 3, 5, 7 | `pool/circuits/notes.nr`, `pool/notes.ts`, `pool/scope.ts`; `model/pool-authority.ts` `ProofOracle` | `scripts/pool/check.mjs`, `pool-scope`, `model/pool-authority.test.ts` | core |
| Signed terms and the witnessed replacement chain determine every scoped term. Authority checks own the complete input before adapter callbacks and require a common declared venue and configuration, exact full-scope links, and historical indices within the read view. Clock/view changes raise venue errors, including on failed reads. Same-key reappointment never reuses an old link. Scheduling uses the earliest witnessed term end, not a caller-supplied boundary. | C2.5, C2.10.1–2, C2.10.9 | `pool/authority.ts` `PoolAuthorityView`; `replacement.ts` `successionAhead`; `pool/schedule.ts` | `pool-authority`, `model/pool-authority.test.ts` | core; claimed authority alone does not authenticate headers |
| An Ergo refresh publishes its witnessed index and complete record set only after fetching every required operator. All record reads refuse during refresh. Failure preserves the previous complete snapshot at its old index, or remains unavailable when none exists; failed candidates cannot alter the admitted-replacement memo. | C2.3, C2.5, C2.10.1–2 | `ergo.ts` `sync`, `requireSettled` | `c2-ergo-venue` | core read adapter; refresh failure is never new record evidence |
| A segment fixes domain, venue, operator, opening sequence, and the scope with each backing's link and exact opening; its header's hash is its identity, and an opening by the same operator is below the segment's sequence. Checkpoints finalize the whole scope; only proven term endings lapse a scope; invalid incumbent carriage blocks fallback. | C2.10.1–5, pool-v2 §6, §9 | `pool/statement.ts` `segmentBytes`, `isWellFormedHeader`; `pool/segment.ts` `directory`; `pool/descent.ts`; `model/pool-authority.ts` `World.include` | `pool-statement`, `pool-segment`, `pool-descent`, `model/pool-authority.test.ts` | core; runtime finality in `pool/checkpoint.ts` |
| Exact descent selects before replay: each eligible held step needs authenticated directory absence or snapshot-bound public whole-scope lapse to pass. Child-relative rank permits earlier indices, or lower signed sequences by the same operator at the child's index. Historical terms exclude out-of-force records and preserve intervening terms across reappointment. Withheld evidence stops; live invalid history never licenses fallback. | C2.7.1–3, C2.10.4, pool-v2 §9 | `pool/descent.ts` `readPoolPredecessor`; `model/pool-authority.ts` `predecessorFor` | `pool-descent`, `model/pool-authority.test.ts` | core candidate selection; replay and transitive canonical finality still required |
| A segment imports one finalized prefix per distinct opening commitment, each checked against itself and against the opening's directory root and operator; events deduplicate by (segment, position) with identical effects; a nullifier or output in two distinct events, a total out of bounds, an unnamed prefix or an opening without one is refused. The forest is the prefixes' roots plus the fresh local tree's; one spent set and one output set cover the closure and the tail; totals fold over the deduplicated closure. Witnessed-index ranks, canonical predecessors and finality are the record's. | C2.10.5–7, pool-v2 §10 | `pool/segment.ts` `mergeImports`, constructor, `prefix`; `model/pool-authority.ts` `World.open`, `import`, `merge` | `pool-segment`, `scripts/pool/admission.mjs`, `model/pool-authority.test.ts` | core; runtime ranks in `pool/checkpoint.ts` |
| A held checkpoint finalizes every scoped backing together: select each canonical predecessor before replay, validate all opening checkpoints transitively at decreasing witnessed ranks, and preserve the same-segment finalized prefix. Missing history stops; live invalid history blocks fallback. Recomputed directories bind the entire scope. Historical finality survives term endings. | C2.10.3–5, pool-v2 §10 | `pool/checkpoint.ts` `readPoolCheckpoint`; `pool/descent.ts`; `pool/segment.ts` | `pool-checkpoint`, `model/pool-authority.test.ts` | core; read-only historical finality |
| Before a child exists, current descent includes all eligible held sequences at the witnessed index and preserves historical term ends. Opening construction derives the scope and exact canonical imports, validates shared ancestry once before any verifier callback can mutate other roots, and starts empty local history. Its next sequence is the durable operator-wide highest signed plus one, including declined publications; the record is only a lower bound. Preparation grants no discard or signing authority. | C2.4.1, C2.7.3, C2.10.4–9, pool-v2 §6 | `pool/descent.ts` `readPoolCurrent`; `pool/opening.ts` `preparePoolOpening`; `pool/checkpoint.ts` `readPoolCheckpoints` | `pool-opening`, `model/pool-authority.test.ts` | core; read-only preparation |
| Before elective scope changes, witness all live receipted events and the latest signed checkpoint. Restarts preserve the journal; retired instances cannot serve. Actual term endings or stale signed states permit canonical reopening while retaining historical receipts. | C2.10.8–9, C2.8 | `pool/store.ts` `activate`, `load`; `model/pool-authority.ts` `Service`, `World.open` | `pool-store`, `model/pool-authority.test.ts` | core; Node 24 durable store |
| Journal each opening, accepted statement and signed reply atomically before exposing it. Retain complete used canonical evidence, including same-segment predecessors and history-free absence/lapse steps, original receipts and failed-publication sequences. Revalidate imports before new service after reload. One database owns one operator key on its venue; resuming it fences older handles. | C2.4.1, C2.8, C2.10.8–9, pool-v2 §9 | `pool/store.ts`, `pool/store-codec.ts`; `pool/evidence.ts` | `pool-store`, `pool-store-codec`, `scripts/pool/store-crash.mjs` | core; copied-key and whole-database rollback prevention remain party obligations |
| An optional configured segment constraint compares every retained opening inside the ownership transaction before fencing, and compares a prepared activation before signing. It never replaces authority or checkpoint verification. | C2.8, C2.10.8–9; local application constraint | `pool/store.ts` `requiredSegment`, `requireSegment`; `scripts/pool/local/` | `test/pool-local-store.test.ts`, local profile/path and real-proof acceptance | exact initial single-backing profile; no protocol bytes or journal schema change |
| Notes bind domain, backing, value, owner and rho; ownership and an anchor-independent nullifier are proven for real and padding inputs; a positive input proves depth-32 membership at its own anchor with child-level node tags; every input proves depth-16 scope membership under its link with the scope's own leaf and node tags. | pool-v2 §§1, 3–5, 7 | `pool/circuits/notes.nr` | `scripts/pool/check.mjs` | core |
| Issue, spend and burn expose exactly 9, 11 and 13 fields, each beginning with the domain, the segment identity and the scope root. Spend takes one anchor per input, conserves each backing in widened integer sums, restricts padding and outputs to the inputs' backings, and requires distinct nullifiers and outputs. Burn destroys a positive public quantity of one scoped backing. | pool-v2 §7 | `pool/circuits/{issue,spend,burn}.nr` | `scripts/pool/check.mjs` | core |
| Circuit sources, compiled bytecode and ZK verification keys match the recorded identities under the pinned toolchain. | pool-v2 §§2, 12, 15 | `pool/circuits/manifest.json` | `npm run check:pool` | core |
| The in-circuit hash `H` is Poseidon2 over BN254, width 4, rate 3, in noir-lang/poseidon v0.3.0's sponge. The host implementation is pinned to Barretenberg's permutation vector and recorded hash outputs, and `check:pool` compares it with the backend. | pool-v2 §1 | `pool/poseidon2.ts` | `pool-poseidon2`, `scripts/pool/admission.mjs` | core |
| A field element is canonical below `p`, written as 32 big-endian bytes or `0x` and 64 lowercase hex digits; an identifier enters a circuit as two limbs below `2^128`; a value is a `u64`. Anything else is malformed. | pool-v2 §1, §13 | `pool/field.ts` | `pool-statement` | core |
| `owner = H(T_OWNER, secret)`, `cm = H(T_NOTE, domain, backing, value, owner, rho)`, `nf = H(T_NULLIFIER, domain, cm, secret)`, each nonzero; the nullifier reads neither operator, segment, anchor nor path. | pool-v2 §3 | `pool/notes.ts` | `pool-poseidon2` | core |
| Each segment's note tree is depth 32 over `H`, append-only, each node tagged with its children's level, unused leaves zero; the accepted-root forest is `z_32`, the segment's own roots and every root certified by its imported prefixes; a wallet builds its own paths from a segment's leaf list. | pool-v2 §4 | `pool/note-tree.ts`, `pool/segment.ts` `isAnchor` | `pool-note-tree`, `pool-segment` | core |
| The scope is 1 to 2^16 entries strictly ascending by backing name, each `(backing, link)`; `leaf = H(T_SCOPE_LEAF, backing, link)`, nodes `H(T_SCOPE_NODE, level, left, right)`, depth 16, unused leaves zero; a scope path is 16 siblings with direction bits. | pool-v2 §5 | `pool/scope.ts` | `pool-scope` | core |
| The spent set is a 256-high sparse Merkle tree over SHA-256 keyed by the nullifier's bytes, most significant bit at the root, its root a function of the set alone; a proof is a 32-byte map of omitted empty siblings plus the rest, malformed where map and siblings disagree; membership and non-membership verify in the clear. | pool-v2 §11, inv 23 | `pool/spent-set.ts` | `pool-spent-set` | core |
| The successor's spent root is a canonical compressed binary tree of full-key leaves, nonempty children and absolute first-differing-bit branches; one set has one root under any validated import/insertion order, updated at every statement. Non-membership remains possible without selecting a published proof format. | [pool-spent C1.2.8–9](https://github.com/mediumofexchange/money-from-first-principles/blob/78f8a8c/pool-spent.md), inv 23 | `scripts/pool/spent-set/radix.mjs` | `scripts/pool/spent-set/check.mjs`, `bench.mjs` | selected successor candidate only; atomic statement/import validation and final v3 adoption remain open |
| The configuration frames six circuit identities, the helper and four bounds, and its hash is the domain; `segmentBytes` frames domain, venue, operator, sequence and the sorted entries with their openings, and `segmentIdentity` is the segment's identity; `statementBytes` frames domain, kind and public inputs, and `statementHash` is the statement's identity; `historyHash` chains statement, note root, spent root and position from a genesis that names the segment; the snapshot digest is backing, segment, history hash, issued and burned. | pool-v2 §§2, 6, 7, 9 | `pool/statement.ts` | `pool-statement` | core |
| **E** names the construction `moe/pool/v2` and the domain as evidence clause `0x05`, inside the name, beside the original operator; a construction the reader cannot check is refused; the transparent ledger refuses a pool backing. | C1.3, pool-v2 §2 | `backing.ts` | `pool-evidence` | core |
| Admission runs §8's checks in order against one committed view — shape, this domain, this segment and this scope root; the proof under the configuration's key for the kind; a scoped backing whose terms are held (E's original operator not compared), K's signature over `statementBytes` and the supply bounds; every anchor in the forest and unspent, distinct nullifiers; new, distinct, fitting outputs — then changes state as one transition. An exact resubmission returns the prior record; a refused statement leaves no trace. | pool-v2 §8, inv 26 | `pool/segment.ts` `admit` | `pool-admission`, `scripts/pool/admission.mjs` | core |
| Replay recomputes every root, the spent set, the history hash, the directory and `outstanding` per backing from the segment's trail and the evidence for its openings — each opening's commitment verified, its directory root matched, its segment replayed to the checkpointed length and its directory compared entry for entry, transitively, once per checkpoint, refusing a cycle or a missing ancestor — verifying every proof and issuance signature, and accepts nothing it did not recompute; a replayed segment proves itself and its supplied ancestry. | pool-v2 §10, inv 12 | `pool/segment.ts` `replay` | `pool-segment`, `scripts/pool/admission.mjs` | core |
| A pool receipt signs exactly §9's frame: domain, segment identity, scope root, position, statement hash, history hash, admitted proof/signature hashes, and `after` (the last signed commitment's sequence, 0 for none). Verification pins the caller's segment authority — domain, identity, scope root, operator; statement identity, exact evidence and replayed history inclusion are separate questions. None establishes witnessed finality or that the scope's links were in force. | pool-v2 §9, C2.10.8, C2b.4 | `pool/receipt.ts` | `pool-receipt`, `scripts/pool/admission.mjs` | core |
| Resolve a pool receipt's exact signed sequence independently of its complete scope's term bounds. Same-index lower sequences remain held; unreached sequences have no distance bound; reappointment never revives an old scope link. | C2.3.4, C2.10.8–9, C2b.4 | `pool/receipt-record.ts` `readPoolReceiptRecord` | `pool-receipt-record` | record facts only; held sequence does not certify segment carriage |
| Check receipt position, statement and history identity against canonical replay of an exact source checkpoint. Later scope endings cannot erase inclusion; missing history remains unavailable. Absence from one prefix is not global lapse or fault evidence. | C2.10.3–5, C2.10.8–9, pool-v2 §9 | `pool/receipt-record.ts` `readPoolReceiptCheckpoint`; `pool/checkpoint.ts` | `pool-receipt-record`, `pool-checkpoint`, `pool-revocation` | inclusion with prospective revocation checked; silence recovery remains unsupported |
| A held receipt reference can lapse at the first canonical empty opening of another segment that carries a scope backing, after a hole between the segment's last held checkpoint and that opening. Original terms remain live at that boundary; complete canonical interval evidence preserves inclusion and contradiction independently, including occupied-position conflict at after; a passed-over checkpoint carrying none of the scope occupies its sequence and is neither a hole nor a transition. Missing evidence never proves lapse. | C2.10.9a, C2b.4 | `pool/receipt-repair.ts` `readPoolReceiptRepair`; `pool/receipt-walk.ts` | `pool-receipt-repair`, `pool-store`, `model/pool-authority.test.ts` | at the supplied repair boundary only |
| The present verdict walks the operator's held commitments through bounded predecessor reads: the segment's checkpoint at after, authenticated whatever its index, or its latest live one below, then every held commitment above, stopping at the earliest term end or the first carrying checkpoint of another segment. It relates and replays those checkpoints in ascending order, ending at the first inclusion, replays the transition only where neither inclusion nor a proven contradiction has decided, and reads final, then contradicted, then abandoned, then lapsed (moved past, repair, scope boundary), then pending. A position occupied otherwise at or below after contradicts; omission above a moved-past after does not; a failure carries the contradictions already proven. | C2.10.9b, C2.10.9, C2b.4 | `pool/receipt-status.ts` `readPoolReceiptStatus`; `model/pool-authority.ts` `classify` | `pool-receipt-status`, `pool-store`, `model/pool-authority.test.ts` | core read; no discard, service or recovery authority |
| The verifier holds the three keys derived from the pinned bytecode and verifies against nothing else; a key supplied with a statement is never accepted. | pool-v2 §§2, 12 | `pool/barretenberg.ts` | `scripts/pool/admission.mjs` | core |
| Quantities, counts, positions and payout arithmetic are `bigint`; `number` only for lengths and indices. | — | everywhere | `encoding-primitives` | core |
| A backing's name is the hash of the canonical encoding of (K, P, R, E); same fields, same bytes, on every machine. | inv 1 | `backing.ts` `backingName` | `invariant-01` | core |
| A backing exists only with a valid strict (non-ZIP215) signature by K over its own name; K is a non-small-order point; identity is recomputed at signing, verification and registration, never trusted from a cached field. | inv 2 | `backing.ts`, `keys.ts` `verifySignatureStrict` | `invariant-02` | core |
| Issuance and reissuance never share a code path: issuance changes the outstanding count and needs K's signature; movement does neither. | inv 7 | `ledger.ts` | `invariant-07` | core |
| No clawback, reversal, freeze or privileged move exists as a path. | inv 8 | `ledger.ts` | `invariant-08` | core |
| Fees are ordinary transfers, never a shaved reissue. | inv 9 | `ledger.ts` | `invariant-10` | core |
| `outstanding = issued − burned` per backing at every published moment; presentation destroys nothing. | inv 10 | `ledger.ts` | `invariant-10` | core |
| No cycle detection; a reliance cycle would need a hash cycle. | inv 5 | `closure.ts` | — | core |
| `closure(S)` expands deterministically before hashing, counts sum where paths meet, size is capped; `closureOf` is a tool for writing terms, `closureStatus` is what a reader asks. | inv 16, §8b | `closure.ts` | `invariant-16` | core |
| Quantities are whole units; R is a conjunction over a fixed list with constant whole counts naming backings and chain assets only. | inv 13–15, 18 | `backing.ts`, `presentability.ts` | `invariant-13.*` | core |
| An unaccompanied claim is inert, never invalid, and still transferable. | inv 17 | `presentability.ts` | `invariant-17` | core |
| Time is a witnessed venue index, never a clock; one evaluation instant per presentation, named in the demand, agreed by the acceptance, no later than the latest witnessed index at signing. | inv 21, 24 | `venue.ts`, `sequencer.ts` | `invariant-21`, `invariant-24` | core |
| Every time rule is a refusal, never a balance change, so an honest history stays replayable. | decision 2026-08-21 | `sequencer.ts` | `c3-lock-timeout` | core |
| Every state a sequencer asserts proves against its latest commitment; the root is injective over the issuance log, spent set, totals, standing demands and pending locks. | inv 22, 23 | `commitment.ts` `stateRoot` | `invariant-22`, `invariant-23` | core |
| A commitment authenticates a sorted directory of (backing name, snapshot digest). A name present without its log is unavailable evidence; absence from the directory proves the commitment does not carry the backing. Directory framing v1 (`MOED`), signature context `moe/commitment/v2`; earlier roots are incompatible. | §C0b, C2.4.2 | `commitment.ts` | `commitment-directory` | core |
| A receipt names the commitment its operator last *signed*, never a witnessed index; the record answers *holds* (by exact sequence), *not reached*, or *moved past*. | C2b.4, C2.3.4 | `receipt.ts` `eraIndex` | `c2b-receipt-era`, `c2b-the-blind-window` | core |
| A replacement is co-signed by its successor and takes force at its effective index; the chain from the original terms is walkable; `successionOf` stops at the last link whose index has arrived. | C2.5.1–2, C2.5.6 | `replacement.ts` `successionOf` | `c2-succession`, `c2-force-is-effective` | core |
| A replacement's effective index is at least twice the venue's lag plus one past its witnessing; the venue's id binds its lag. | C2.5.3, C2.3.5 | `replacement.ts` `admitted`, `venue.ts` `lag()` | `c2-the-lead-time` | core |
| A replacement not strictly later than the link it replaces is void unless it names the incumbent (a revocation); two replacements of one link resolve by C2.5.5. | C2.5.4–5 | `replacement.ts` | `c2-succession` | core |
| A venue's record for one key rises in sequence as it rises in index: it keeps only what strictly extends. | C2.3.3 | `venue.ts`, `ergo.ts` | `c2-venue-records` | core |
| A predecessor read returns the greatest held sequence below an exclusive sequence bound and at or before an inclusive witnessed index. Same-index predecessors remain reachable and sparse sequence holes cost no probes. This is the record-read prerequisite for directory descent, not proof of carriage or pool finality. | C2.3.3–4, C2.7.2 | `venue.ts`, `ergo.ts` `previousFor` | `c2-venue-predecessor` | core read capability |
| One commitment stands in flight; the next waits for the record to show it or for the lag to pass. Its sequence is one past the highest this operator has signed. | C2.4.3, C2.4.1 | `pool/store.ts` `ready`, `activate`, `commit`; `pool/schedule.ts` | `pool-store`, `pool-schedule`, `c2-the-resume` (frozen) | core |
| An operator serves the log its own signed-but-unwitnessed commitment stands on. | C2.4.4 | `sequencer.ts` `serves` | `c2b-the-blind-window` | core |
| A commitment never silently drops an in-force backing. | C2.4.5 | `sequencer.ts` `commit` (`dropping`) | `c2-dropped-backing` | core |
| A key seated anew, or returning from silence, commits before it co-signs. | C2.7.4, C2b.4 | `sequencer.ts` `shut`, `restore` | `c2b-return-from-silence` | core |
| A restarted process resumes from its latest **signed** commitment and the durable co-signed tail; it commits and co-signs nothing until the lag has passed. The frozen sequencer resumes from witnessed state and must not be ported unchanged. | C2.8 | `pool/store.ts` `load`, `ready`; `model/sequencing.ts` | `pool-store`, `scripts/pool/store-crash.mjs`, `model/sequencing.test.ts` | core; Node 24 durable store |
| Takeover. A seat's opening state is the last commitment carrying the backing, by a party then in force, witnessed strictly before the effective index; the record fixes it, never the seat. **Frozen implementation:** `takeOver` descends from the operator's own link, paying each step past a non-carrying commitment with that commitment's *whole served state* as an exhibit, and where the descent cannot be paid the operator signs an *opening claim* in its next commitment (`commit({ opening })`). Construction C2.7.2 now pays each step with a directory absence proof and C2.4.5 retires the opening claim (Appendix). Do not port either retired mechanism to the pool path. | C2.7 | `sequencer.ts` `takeOver`, `serves`, `lastCommitmentInForce` | `c2-the-book`, `c2-the-cold-walk`, `c2b-the-record-is-the-backings` | frozen |
| Every operation is signed by the party the law names, over that backing's own message, at that signer's next nonce — except the n-party commit, which names no backing, carries no nonce, and is signed by every party the lock names. | law, §C1.7 | `ledger.ts`, `messages.ts` | `invariant-06` | frozen (per-holder nonces are the transparent profile's; the pool's replay protection is the nullifier set and inv 26) |
| Swaps and presentation are idempotent: a resubmitted operation returns the identical prior receipt; a different operation at a spent nonce is declined. | inv 26 | `sequencer.ts` | `invariant-26.*` | core |
| Settlement voids the exact claims offered only on the holder's release; dishonour is the branch where no live acceptance answers; an acceptance may not outlast the demand's deadline. | inv 27, §C3 | `presentation.ts` | `invariant-27`, `c3-payout-in-claims` | transparent oracle; the pool contract rows below model the rule over notes |
| The non-service count stands against the backing and the grade names the incumbent; a handover neither resets nor moves it. | C2b.5 | `recovery.ts` `isNonServing` | `c2b-non-service` | transparent oracle; the pool contract rows below model the rule over notes |
| The no-commitment clock is the backing's: it runs from the last commitment by a party then in force, or from index zero, and only a commitment closes it. | C2b.6 | `recovery.ts` `isSilent` | `c2b-silence-and-recovery` | core |
| A revocation is per venue, effective at its witnessed index, prospective. | C2b.1 | `revocation.ts` | `c2b-revocation` | core |
| Newly finalized local issuance must precede revocation strictly; later checkpoints preserve an exactly matching validated prefix. Imported issuance retains its source finality. Invalid or unavailable live ancestry cannot be skipped, and same-index revocation changes invalidate a read. | C2b.1, C2.10.3–6 | `pool/checkpoint.ts`; `model/pool-authority.ts` | `pool-revocation`, `model/pool-authority.test.ts` | core; no filtering or repair of invalid committed history |
| The durable pool store refuses new revoked issuance and active/imported histories whose issuance lacks proven pre-revocation finality. Later imported checkpoints preserve earlier issuance through retained, revalidated predecessor evidence. Cutoff changes during signing abort the append. | C2b.1 | `pool/store.ts` `unrevoked`, `checkRevocations`, `validateOpening`, `revocationView` | `pool-store` | core revocation; silence recovery remains next; see `docs/POOL_STORE.md` |
| The snapshot a holder redeems against is the backing's: the last carrying commitment by a party then in force, whichever term it fell in. | C2b.3 | `recovery.ts` | `c2b-redemption-legs`, `c2-dropped-backing` | transparent oracle; pool recovery is not implemented |
| Shielded recovery preserves positive holdings in canonical finalized history and accounts for subsequent valid recovery settlements; unwitnessed spends and receipts create no recoverable holding and cannot redirect redemption. No spend-based challenge window applies. Settlement preserves holder consent and outstanding supply through backer-owned replacement notes. | C2b.3a–c, C2b.4 | `model/pool-authority.ts` (existing finalized state) | `model/pool-recovery.test.ts` | model covers the finalized-history boundary; the rows below specify and model the objects; nothing at wire level, unimplemented |
| Presentation over the pool: a demand names whole notes by tag and locks them until its own deadline, and only its settlement or its withdrawal discharges it from the standing-demand record; a spend or burn of a tag under a standing lock is refused, and a spent tag voids its demand; only the demand's own settlement, on K's acceptance naming a public owner value and the presenter's release, consumes exactly the demanded notes to the backer; a withdrawal, itself a signed statement bound to its segment, frees them; the history binds standing demands and locks; the instant is a window below the index the act would first be witnessed at, and deadlines are read there too. | C3.1–8 (pool-recovery.md) | `model/pool-authority.ts` `applyEvent`, `lockStanding`, `ProofOracle.verify`; `model/pool-recovery.ts` `serving` | `model/pool-recovery.test.ts` C3.7–C3.8, review dispositions | model only; `moe/pool/v3` objects, no v2 runtime path |
| The non-service count: distinct tags of proven segment-free requests whose identity was first witnessed naming this backing in `[t − W, t − d]`, valid against the canonical state at `t` and neither spent nor locked there; copies do not renew the window, a proof-bound 64-bit refresh permits holder renewal under the same anchor. Fires at `m` against the incumbent; handover neither resets nor moves it. | C2b.5.1–2 | `model/pool-recovery.ts` `count`, `model/pool-authority.ts` `ProofOracle` | `model/pool-request-replay.test.ts`, `model/pool-recovery.test.ts` C2b.5 | ideal model only; wallet refresh derivation/persistence and v3 circuit remain open |
| The gap opens after the declared duration from the last commitment by the party in force other than a whole-scope lapse, and one scope declares one duration; the snapshot is the backing's last carrying canonical checkpoint, and invalid live evidence blocks the read; a publication is read for the backing its statement names and has force once, at its own index, in an open gap, against the snapshot's forest and spent set and every settlement since the snapshot's adoption index; bare nullifiers and other statements have none. | C2b.6.1, C2b.3.1–3 | `model/pool-recovery.ts` `gapOpen`, `snapshot`, `adoptionIndex`, `recoveryState`, `open` | `model/pool-recovery.test.ts` C2b.6.1, C2b.3, review dispositions | model only |
| Return requires a new segment after the first scoped gap strictly after its opening, even following an unrelated clock reset. A continuation also lapses in a current gap at the opening's own index. Earlier finality and liability survive; otherwise unfinished receipts lapse at the earlier silence/term boundary. Actual silence authorizes discard; a predicted gap does not. The new segment adopts every publication with force through its opening index before other statements. | C2b.4.1–3 | `model/pool-recovery.ts` `receiptSilence`, `passedByRule`, `block`, `adoptable`, `replayed`, `discardable`; `model/pool-authority.ts` `classify`, `classifyRepair`, `Service.adopt` | `model/pool-recovery.test.ts`, `model/pool-recovery-return.test.ts`, `model/pool-silence.test.ts` | model only; authenticated production interval evidence remains required |
| Conflicting signed histories are provable fault: two signatures at one nonce, a double position, a double acceptance, a rewritten history. | inv 22 | `fault.ts` | `provable-fault` | core |
| A venue's refusal is not a verdict: `VenueError` propagates through every verifier that takes a `Venue`, in one place (`answering`); the pilot boundary maps it to `unavailable`. | design | `venue.ts` `answering`, `pilot-store.ts` | `venue-refusal`, `pilot-store` | core |

### Presentability and the lock (inv 13–15, 18; §C3)

- A demand reserves each leg with a `lock` in that leg's own log, so every backing stays replayable alone; the sequencer takes the demand and its locks as one act or none (single-phase).
- Two-phase across operators is `submitLock` + `settle`: the holder reserves at each sequencer, publishes one commit at a decision venue, and each settles its own half against that witnessed object. The n-party exchange (§C1.7) is the same mechanism: a sequencer converts its lock only when every party the lock names has signed.
- A lock names who may convert it. One party converts on their release or commit; several only on the witnessed object. A set leg names no decision venue (`NO_DECISION_VENUE`) and settles only with its set.
- A bundle lock is prepared only where the sequencer can read the commits it may settle on, and only for an attempt the record does not show committed.
- Two-phase is not the only honest answer: partial-and-retry (§C2.9) is the ordinary transfer path; which branch a trade uses is the parties' choice.
- Where P pays in claims, the backer's acceptance reserves the payout — its lock on the paying backing, to the holder, convertible by the holder alone — and the release settles set and payout as one act (`payoutOf`).
- Whether a demand's legs were locked is read across the served state by `accompanimentOf`; converter and venue are one definition, `LegTerms`.
- A lock carries §C3's timeout, and `lockIsLive` is the one predicate both exits read: at or before it a commit can settle and no withdrawal is accepted; past it withdrawal is the exit, unless the record shows a commit witnessed in time (`committedInTime`). Exactly one exit is open at every index.
- A leg that lapses inside a live acceptance is re-prepared through `submitLeg`, one leg per call; a demand outlives its locks. A deadline and a timeout are strictly ahead of the index at filing.
- A lock is keyed by (attempt, holder); a release or withdrawal names the record it ends. A venue-naming attempt is named by the hash of its terms, `H(salt ‖ venue ‖ timeout ‖ parties)` (`attemptIdOf`), so one attempt carries one timeout across backings and operators; the salt is drawn at random by the party.
- An entry's identity is its signed message, plus — for a commit alone — its signatures (`opIdentityOfEntry`); one answer serves the receipt, the committed root and the rewritten-history comparison.

## What the parties must do, that no code here enforces

Rules the protocol cannot check but the reference must not leave unsaid. Each cites the specification rule it follows from.

1. **Restart in order** (C2.8, C2.10.9): restore the latest signed commitment and every co-signed statement after it, register the complete scope, wait the venue lag, then let the commit timer run. An elective scope change first witnesses the live tail. Retain signed evidence after a forced lapse; missing history is not permission to serve an earlier state.
2. **One writer at a time, or a threshold key** (C2.5.8). Two live processes holding one operator key co-sign conflicting operations, and `fault.ts` proves that as if it were malice. A t-of-n threshold with t > n/2 removes the possibility rather than recording it.
3. **A threshold K, or theft is unbounded** (C2b.1). Revocation is a stop-loss, not a remedy.
4. **The payee keeps the signed request and the receipt** (C2b.4). During a gap the receipt is the only evidence outside the operator's log that an operation was accepted, and the request is what makes it resubmittable. Read the receipt's era: one naming a commitment below the record's, or an era before the operator's current seat, is stale on its face.
5. **Do not treat an unwitnessed payment as a recoverable holding** (C2b.3a–c). Shielded recovery preserves finalized holdings; a pending receipt cannot redirect the payer's snapshot redemption. Keep the evidence of operator acceptance, but do not infer compensation or operator fault from darkness alone. The transparent profile alone retains its limited challenge window.
6. **Keep the last committed state, and its directory, that carries your backing** (C2.7.2, C2b.5). Every remedy against an operator that drops one backing runs through that state, and the party holding it has the motive not to serve it.
7. **A payment is final when witnessed, not when co-signed** (C2.9, C3). An operation accepted after the operator's last commitment lives only in its unpublished log and dies with it. A set is one act and dies as one; perform only against the witnessed whole.
8. **Handover conduct** (C2.6.1, C2.10.9): keep the last signing clock before the earliest scope boundary free (`effective − lag − 1`); refuse work that cannot be committed within every scoped term. The earlier admission cutoff does not discard a tail. `pool/schedule.ts` supplies these time checks; record authority and durable execution remain the sequencer's.
9. **Draw a fresh random salt per attempt** (§C3). A commit binds its id and nothing else, so an object you signed converts any later lock under that id whose parties you are among.
10. **Check your counterparty's lock stands under the attempt you agreed** before you sign the exchange object.
11. **Open a segment only from the record's openings** (C2.7, C2.10.4): the header names each backing's canonical predecessor and its current link as the witnessed record fixes them; `preparePoolOpening` constructs this state from the current record; durable activation must recheck currency, preserve live obligations and commit the opening before receipts.

A receipt proves **acceptance, not a holding**: it attributes an act to the operator and never proves a value to a holder.

## Design rules

The invariants above say *what* must be true. These say *how* to build it. The goal is a reference implementation an auditor can read once and be convinced by: **smallest, then most secure, then fastest — in that order when they conflict, except that security never loses to size.**

**One mechanism per property.** If a property is enforced in three places, an auditor must check three places and a maintainer can break it in three ways. When a fix is needed, first ask whether an existing mechanism should be generalised. Never layer a second mechanism on a first to patch its gap; that is how a review finding becomes permanent complexity. This is Construction §C0a applied to code, and the specification holds itself to the same test.

**Say it plainly.** Names in code, comments and tests use Construction's words: commitment, directory, record, effective index, opening state, nullifier. No new metaphors. Cite rule numbers (`C2.5.3`) rather than quoting paragraphs.

**Bytes are framed, not concatenated.** Every field in a signed or hashed message is fixed-width and asserted to be, or length-prefixed. Two different values must never produce one byte string. Use `ByteWriter.key32` / `ByteWriter.fixed` for fixed-width fields, except `pool/spent-set.ts`, which fills two preallocated frames and asserts each field itself, more strictly than `fixed` does.

**Validate once, at the boundary that owns the rule.** `makeBacking` owns backing well-formedness; the ledger or segment owns the law and funds; the sequencer owns routing and refusal. A layer does not re-check what a layer below will check, and does not pre-check in order to relabel an error.

**Copy on the way in, copy on the way out.** Bytes entering validated state are copied once at construction; every accessor returns a copy. `readonly` is erased at runtime and is not a boundary.

**Verifiers never throw.** Anything that answers a question about adversary-supplied data returns `false` or a typed rejection on any malformed input. A verifier that throws is a denial-of-service hole and tempts a caller to read "no exception" as "checked".

**But a venue's refusal is not malformed input.** A real venue holds a partial view and refuses what it was not synced for; answering `false` there states a fact about a party built out of not having looked. So `VenueError` propagates where everything else is caught, in one place — `answering` in `src/venue.ts` — and `venue-refusal.test.ts` holds every verifier that takes a `Venue` to it.

**An error names the boundary that refused.** `EncodingError`, `SigningError`, `LedgerError` (`NonceError`), `SequencerError`, `VenueError`, `PilotError`, `PoolError`. Do not add one without a new boundary to name.

**Domain tags live in one file.** Every context string that separates one signed message type from another is declared in `src/contexts.ts`, and the prefix-free property is asserted at load. A tag collision is a signature-forgery class.

**Cryptography is hashes, signatures and the declared proof system, and nothing else.** `@noble/hashes`, `@noble/curves`, and the pinned circuit backend the pool declares in **E**. A new primitive is a decision, not a dependency bump.
