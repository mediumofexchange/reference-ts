node_modules/
dist/
*.log
*.tgz

# Scratch verification scripts — the .mjs probes that prove a bug and then
# prove the fix. Gitignored, and kept in scratch/ so the repo root shows only
# the durable project files. Supported implementation code lives in src/.
/scratch/
/*.mjs
